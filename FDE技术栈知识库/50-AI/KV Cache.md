---
title: KV Cache
aliases: [KV缓存, Prompt Cache, 前缀缓存, 键值缓存, 注意力机制]
tags: [fde, ai, llm, cache, performance]
created: 2026-09-06
---

# KV Cache

> [!abstract] 定位
> **KV Cache** 把已计算 token 的 Key/Value 向量缓存下来，下一轮只计算新增 token——省去随上下文长度平方级增长的前缀重算。对 Agent 开发者的核心价值：**前缀字节级不变 = 缓存命中 = 更快（TTFT ~0.5s vs 3-5s）+ 更便宜（缓存读取约为普通输入的 1/10）**。在生产级 Agent 系统中，缓存不只是性能优化——**它是一个架构约束**，决定许多看似无关的设计决策。

---

## 一、注意力机制：KV Cache 的基础

模型读到"北京的天气怎么样"的"怎么样"时，需要决定前面哪些词最重要。三个向量完成"找重点"：

| 向量 | 含义 | 在这个例子中 |
| --- | --- | --- |
| **Query（查询）** | 当前词发出的"搜索请求" | "怎么样"问：哪个词和我最相关？ |
| **Key（键）** | 每个词的"标签"，用于被搜索匹配 | "北京"偏向地名、"天气"偏向气象 |
| **Value（值）** | 每个词的"内容"，匹配成功后被提取 | 匹配到"天气"后提取其语义信息 |

计算三步：当前词生成 Query → 与所有 Key 做点积（匹配度打分，权重归一化总和=100%）→ 对所有 Value 加权求和。注意力热力图呈**三角形**——每个词只能看到自己和前面的词（因果），不能"偷看"未生成的内容。

**注意力可视化实验（实验 2-2）的关键发现**：
1. **注意力储存池（Attention Sink）**：序列第一个 token 常吸收超过 70% 的注意力权重——softmax 硬性要求所有权重的和恰好等于 100%，模型无法表达"不关注任何东西"，必须为"剩余权重"找稳定容器，序列开头成了最自然选择。这是系统性现象而非缺陷。
2. **思考的三角形**：思维链（`<think>` 内）频繁回看之前的思考与工具定义。
3. **输出的三角形**：思考结束后用思考过程作为提示输出回答。
4. **位置偏好（Position Bias，"Lost in the Middle"）**：对开头和结尾的信息分配更高注意力，中间易被忽视——**最关键的信息放开头或结尾**是重要实践原则（状态栏放末尾正是应用）。

---

## 二、KV Cache 的原理与约束

无缓存：第 6 轮对话、上下文 2000 token——每生成一个新 token 都要重算全部 2000 token 的 K/V（重跑整个前缀的前向计算），prefill 阶段计算量随上下文**平方级**增长，几十轮工具调用的任务不可接受。

有缓存：历史 K/V 算过一次即缓存。生成新 token 只需算自己的 K/V，与缓存一起完成注意力。**注意**：省去的是历史 token 的 K/V 投影重算；每个新 token 的注意力仍要遍历全部缓存 K/V，计算量随上下文长度**线性**增长——这正是长上下文解码越来越慢、KV Cache 显存与带宽成为推理瓶颈的原因。

**为什么修改前缀会导致变动点后失效**：Transformer 数十到上百层串联——第 1 层输出喂第 2 层……第 k 个 token 变化（哪怕系统提示词改一个字），k 之前不受影响，k 之后的表示逐层受影响。**缓存只能保留到首个不同 token 之前，从该位置起重算。代价取决于改动位置：变动点越靠前，重算与计费越多**（实验实测可达数倍）。

性能对比（3000 token 总上下文）：

| | 缓存命中 | 后缀缓存失效 |
| --- | --- | --- |
| TTFT | ~0.5 秒 | 3-5 秒 |
| 成本 | 仅新 token 计费 | 变动点后的 token 重新处理 |

---

## 三、Chat Template：API 消息 → Token 流

API 的结构化 JSON 消息由 **Chat Template** 转换为模型实际处理的线性 token 流——像信封格式：用特殊标记（`<|im_start|>system`、`<|im_end|>`）划分每条消息的角色与边界；不同模型家族（Qwen/Llama/Gemma）用不同"信封格式"。API 服务端（vLLM/Ollama）自动完成，开发者通常不手动处理。

**对 Agent 开发者的两个实用价值**：

1. **解释了为什么必须用标准 API 格式**。绕过 API 自行拼接（如把工具结果作为普通 user 消息而非 tool 类型），Chat Template 会误判"新的用户查询"，**误触发思维链清理**——Qwen3 在多轮工具调用中会把之前 `<think>` 内的思考像草稿纸推导一样保留；检测到新用户查询时默认"换了话题"清空重算。工具结果被错误标为用户消息 = "模型正算到一半，草稿纸被收走了"，严重影响多步思考连贯性。
   - 各模型历史思维链策略差异大且快速演变：**DeepSeek R1 剥离全部历史思考**（只回传 content——R1 训练时历史 CoT 从不出现在输入，塞回去是分布外输入，还省 token；但对 Agent 有缺陷：中间思考承载"为什么调这个工具、排除了哪些假设"，剥离后每轮从零推理，易重复犯错丢长程计划）；**DeepSeek V4 彻底反转为强制回传全部 reasoning_content**（否则报错），Kimi K2、GLM-5 同协议；**Claude 要求 thinking block（带签名校验）原样回传**，服务端忽略最后一次用户输入之前的思考。**用前查对应模型最新文档。**
2. **解释了 KV Cache 为什么对前缀如此敏感**：system 消息和工具定义转为固定 token 序列放最前，K/V 缓存后跨请求复用；前缀一个 token 变（哪怕一个空格），首个不同 token 起的缓存全部无法复用。

---

## 四、三条核心结论（必须掌握，原理可跳过）

1. **系统提示词和工具定义一旦确定就不要改**——任何改动哪怕多一个空格都可能改变 token 序列，使首个不同 token 及其后的缓存无法复用；改动越靠前，延迟和成本影响越大。
2. **动态信息永远追加到末尾**——时间戳、用户状态等变化的内容作为新消息追加，而不是修改已有的系统提示词。
3. **使用标准 API 格式，不要自行拼接消息**——结构化消息由 Chat Template 翻译为训练时见过的固定 token 序列；拼成 "USER: ... ASSISTANT: ..." 的根本问题是偏离训练格式。缓存本身只认 token 字节序列，拼接只要字节级稳定照样命中，但若拼接方式不稳定（每次向前缀注入动态内容）缓存随之失效。

---

## 五、常见的错误上下文管理模式（实验 2-3）

1. **动态系统提示词**——开篇的时间戳故事：客服 Agent 每天 10 万次对话，工程师加了一行 `Current time: {{now}}`，次日监控告警：首 token 延迟 0.5s → 3-5s，**月度推理账单几乎翻倍**。代码看起来完全没问题，模型也没换。正确做法：时间信息追加为末尾用户消息，或需要时工具调用获取。
2. **动态用户配置**——每次请求更新剩余调用次数/账户余额。改用专门的状态管理机制按需处理。
3. **工具定义的动态排序**——按使用频率重排工具；工具定义占很大篇幅（每个数百 token），改变顺序从首个变动工具起全部失效。实验：保持固定顺序对模型选择工具能力**几乎没有影响**，对性能提升显著。
4. **滑动窗口对话历史**——只留最近 10 条。两个严重问题：破坏前缀一致性（缓存失效）+ 丢失关键工具调用结果（第 2 轮读的文件内容第 15 轮还要引用，原始结果已滑出窗口）。实验中滑动窗口的 Agent 经常陷入循环，反复执行相同工具调用——它"忘记"了已获得的结果。
5. **文本格式化**——结构化 role-content 消息转为纯文本流。**最具破坏性**：模型训练时接受大量基于角色的对话数据，转为纯文本后要额外消耗注意力推断角色边界——重复执行已完成操作、忽略工具结果、该调工具时生成文本、格式解析错误。

小结：五种错误模式的解法都收敛回三条核心结论。**模型提供商为标准接口做了大量优化，偏离标准格式往往是给自己挖坑。**

---

## 六、KV Cache 与 Prompt Cache：两个层级

| | KV Cache | Prompt Cache |
| --- | --- | --- |
| 层级 | 模型内部机制 | 推理引擎优化（跨请求） |
| 作用 | 加速单次请求内的 token 生成 | 多次 API 请求间复用相同前缀的计算结果 |
| 原理 | 缓存已算 token 的键值对 | 服务端对请求前缀匹配，相同前缀直接复用之前算好的 KV Cache |

Prompt Cache 的缓存读取成本远低于首次计算——**Anthropic、DeepSeek、GPT-5 约为十分之一**。各家启用方式与计费差异不小（有的自动启用、有的需手动指定），使用前查最新文档。

---

## 七、缓存作为架构约束（Claude Code 实践）

当 Prompt Cache 的经济效益足够显著时，**缓存一致性反过来主导系统架构选择**：

1. **提示词的结构由缓存边界决定**。系统提示词被缓存边界标记分为两部分：标记之前可跨用户、跨会话全局缓存；标记之后含用户/会话特定信息。**提示词的排列顺序首先由缓存经济性决定，其次才是语义逻辑**——每个运行时条件（OS 类型、模式、语言）若放在边界之前，缓存键变体翻倍：3 个二值条件 = 2³ = 8 种缓存键。**所有动态元素都放边界之后。**
2. **子 Agent 必须与父 Agent 字节级对齐**。派生子 Agent 或旁路查询时若继承上下文，提示词、工具定义、模型配置、消息前缀、思考配置必须**逐字节匹配**才能命中 Prompt Cache（使用不同上下文/提示词的框架则不要求）。
3. **工具结果的替换字符串在首次出现时就被冻结**。大输出被替换为摘要预览时，替换字符串持久化——即使会话重启也用完全相同的字符串，保证恢复后的消息序列与缓存字节流一致。

核心启示：**缓存经济性不是事后优化，而是前置约束。越早纳入架构设计，后续工程代价越小。**

---

## 八、前沿：可编辑、可组合的 KV Cache（深水区选读）

前面全部建立在一条铁律上：前缀改一个字节，后面缓存全废。**这条铁律在今天的推理引擎里成立，但未必是必然的**。

反直觉的观察（Li, Bojie. "Models Take Notes at Prefill", arXiv:2606.17107）：prefill 阶段模型其实在"做笔记"——读到某字段（"用户所在城市：北京"）时，顺手把"这个字段意味着什么"的结论写进了后面每一层的 KV 状态；**字段自身那几个 token 的 KV 对最终决策的贡献往往不到 1%**——真正影响输出的是它留下的"读书笔记"。由此打开两种操作：

- **编辑（Editing）**：改掉字段后，只要模型有显式思维链，改动可顺着已缓存的思考传播——用约 1% 的算力得到与"整段重算"一致的结果。（边界：没有 CoT 时孤立改字段会被忽略——结论已固化在下游状态，却没有思考路径去更新它。）
- **组合（Composition）**：预计算好的"技能"缓存经旋转位置编码（RoPE）挪到新位置直接拼接，O(L²) 重算降为 O(L) 拼接，结果与完整重算难以区分。

比喻：读厚文档时不每改一个事实就从头重读，靠页边笔记——笔记已记下每个事实的推论，事实变了只需修正那条笔记；笔记用可搬运的速记写成，还能把上次为别的问题记的一页重新编号（RoPE 重定位）粘到新问题里复用。

实测（vLLM）：首 token 延迟（p90）最多降数十倍至数百倍，前缀缓存命中率约 98.5%，输出与逐字重算决策完全一致（跨 12 个模型，logit 余弦相似度 0.90-0.999）。

**对 Agent 的意义**：被反复重建的长上下文——换工具、更新记忆字段、注入状态栏——也许不必每轮推倒重来，指向"**上下文可变、但缓存收益还在**"的可能。对 [[Agent Skills]] 尤其有价值：同一批 skill 跨会话反复在不同位置加载，预编译为可复用缓存对象后用 RoPE 重定位拼接，skill 就从"每次重新 prefill 的文本"升级为"可复用、可组合的缓存对象"。

> [!note] 现状
> 仍属研究阶段；生产系统中三条实践结论依然是应当遵守的默认纪律。

---

## 九、常见坑

> [!warning]
> - **"对 KV Cache 友好"≠"零成本"**：目录首次进入请求需要处理，完整 Skill 正文首次加载也是新增计算——前缀稳定后后续请求才能复用。
> - **动态工具列表的朴素实现**：把全部工具定义放静态前缀、每加载一个新工具缓存全失效——正确姿势是 schema 追加到轨迹（见 [[工具设计原则]] 主动工具发现）。
> - **Prompt Cache TTL 过期**：整段前缀一起重算（并非工具定义特有的代价）。
> - **修改、移除或重排已加载的工具集**：缓存从变动点起失效——发现过的工具不要动。

---

相关笔记：
- [[LLM API]] —— Token 成本与延迟的底层
- [[上下文工程]] —— "静态前缀 + 轨迹"设计纪律的出处
- [[上下文压缩]] —— 压缩与缓存看似矛盾实则互补
- [[Agent Skills]] —— 元数据常驻 + 正文追加的缓存逻辑；可组合缓存对象
- [[工具设计原则]] —— 动态工具发现的缓存友好实现
- [[Coding Agent]] —— 状态栏环境信息的缓存友好注入
