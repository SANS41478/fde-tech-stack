---
title: 企业系统集成
aliases: [企业集成, 内部系统集成, Enterprise Integration, 系统对接]
tags: [fde, integration, enterprise, security]
created: 2026-08-24
---

# 企业系统集成

> [!abstract] 定位
> **企业系统集成** 是 FDE 非常核心的能力：把你的方案连进客户真实的业务系统——CRM、ERP、内部数据库、企业 API，甚至「只有 Excel 和老 Web 后台」的奇怪环境。集成的成败，往往决定项目能不能真正交付。

---

## 一、企业要集成的对象

```text
Salesforce / HubSpot   # CRM
Slack / 飞书 / 企微      # 协作
Jira                   # 研发协作
Notion / 文档           # 知识
Google Workspace / M365 # 办公套件
ERP                    # 财务/供应链
CRM                    # 客户管理
内部数据库              # 私有数据
企业 API               # 自建系统接口
```

> [!note] SaaS vs 内部系统
> [[SaaS 集成]] 是标准云应用的连接；本篇更强调「客户自有 / 老旧 / 非标准」系统的对接，两者手段相通。

---

## 二、必须理解的集成机制

```text
API Key          # 最简单，但安全性弱
OAuth            #  delegated 授权（见 [[OAuth]]）
JWT              # 无状态令牌（见 [[JWT]]）
Webhook          # 事件回调（见 [[Webhook]]）
Access Token / Refresh Token  # 令牌生命周期
Rate Limit       # 限流，必须处理
Permission       # 权限，最小授权
```

典型场景：
```text
客户 CRM
   ↓ API（鉴权）
FDE Solution
   ↓ AI 处理
CRM / Slack / Email
```

---

## 三、FDE 集成的现实挑战

- **系统很老**：没有 REST，只有 SOAP / 数据库直连 / 网页后台（用 [[Playwright]] 顶上）。
- **文档缺失**：靠抓包、试错理解接口。
- **安全审查严**：客户要你证明 Token 不泄露、数据不越权。
- **数据脏**：真实业务数据充满异常，清洗靠 [[ETL 与数据管道]] 与 [[SQL]]。
- **权限复杂**：跨部门数据要按角色过滤（[[RAG]] 的 metadata 过滤也同理）。

---

## 四、安全是底线

> [!warning] 企业集成安全红线
> - **密钥绝不硬编码 / 进仓库**：用密钥管理 + 环境变量（见 [[Git]]、[[Docker]]）。
> - **最小权限**：只申请必要范围与字段。
> - **传输加密**：全程 HTTPS，内网也要考虑。
> - **审计与脱敏**：日志别打全量敏感数据；留操作痕迹。
> - **可逆**：集成要能随时断开，不影响客户主干系统。

---

## 五、常见坑

> [!warning]
> - **低估对接成本**：「调个 API 而已」往往变成数周适配，提前排期。
> - **不处理限流 / 重试**：生产一跑就 429 或重复写。
> - **忽略数据权限**：检索/展示越权，触碰合规红线。
> - **没有降级**：客户系统挂了，你的方案也挂，要有兜底与队列（[[Redis]]）。

---

相关笔记：
- [[SaaS 集成]] —— 标准 SaaS 连接
- [[OAuth]] / [[JWT]] / [[REST API]] / [[Webhook]] —— 具体手段
- [[ETL 与数据管道]] —— 数据搬运
- [[Playwright]] —— 无 API 时的兜底
